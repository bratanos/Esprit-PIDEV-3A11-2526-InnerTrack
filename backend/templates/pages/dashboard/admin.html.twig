{% extends 'layouts/dashboard.html.twig' %}

{% block header_title %}Panneau d'Administration{% endblock %}

{% block content %}
<div class="max-w-7xl mx-auto pb-24" x-data="adminSystem()">
    
    <!-- Tab Navigation: Premium Glassmorphism Bar -->
    <div class="sticky top-0 z-30 mb-10 -mx-2">
        <div class="bg-white/70 backdrop-blur-xl border border-white/20 rounded-[2.5rem] p-1.5 shadow-sm inline-flex items-center gap-1 mx-2">
            <button @click="activeTab = 'overview'" 
                :class="activeTab === 'overview' ? 'bg-primary text-white shadow-md shadow-primary/20' : 'text-on-surface-variant hover:bg-gray-50 hover:text-primary'"
                class="px-8 py-3 rounded-[2rem] text-sm font-bold transition-all duration-300 flex items-center gap-2">
                <span class="material-symbols-outlined text-xl">dashboard</span> Vue d'ensemble
            </button>
            <button @click="activeTab = 'users'" 
                :class="activeTab === 'users' ? 'bg-primary text-white shadow-md shadow-primary/20' : 'text-on-surface-variant hover:bg-gray-50 hover:text-primary'"
                class="px-8 py-3 rounded-[2rem] text-sm font-bold transition-all duration-300 flex items-center gap-2">
                <span class="material-symbols-outlined text-xl">group</span> Utilisateurs
            </button>
            <button @click="activeTab = 'messages'" 
                :class="activeTab === 'messages' ? 'bg-primary text-white shadow-md shadow-primary/20' : 'text-on-surface-variant hover:bg-gray-50 hover:text-primary'"
                class="px-8 py-3 rounded-[2rem] text-sm font-bold transition-all duration-300 flex items-center gap-2 relative">
                <span class="material-symbols-outlined text-xl">chat_bubble</span> Messages 
                {% set msg_cnt = reports|filter(r => r.context == 'MESSAGING')|length %}
                {% if msg_cnt > 0 %}<span class="w-2 h-2 bg-red-500 rounded-full animate-pulse absolute -right-0 top-3"></span>{% endif %}
            </button>
            <button @click="activeTab = 'community'" 
                :class="activeTab === 'community' ? 'bg-primary text-white shadow-md shadow-primary/20' : 'text-on-surface-variant hover:bg-gray-50 hover:text-primary'"
                class="px-8 py-3 rounded-[2rem] text-sm font-bold transition-all duration-300 flex items-center gap-2">
                <span class="material-symbols-outlined text-xl">forum</span> Communauté
            </button>
        </div>
    </div>

    <!-- Tab Content -->
    <div x-show="activeTab === 'overview'" x-transition:enter="transition ease-out duration-300" x-transition:enter-start="opacity-0 translate-y-4" class="space-y-12">
        <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
            <!-- Metric Cards -->
            <div class="bg-gradient-to-br from-primary/10 to-indigo-50/50 rounded-[2.5rem] p-1 w-full box-border">
                <div class="bg-white rounded-[2.4rem] p-8 h-full">
                    <div class="text-[10px] font-bold text-on-surface-variant tracking-[0.2em] uppercase mb-1">Users</div>
                    <div class="text-4xl font-extrabold text-primary font-headline tracking-tighter">{{ totalUsers }}</div>
                    <div class="mt-4 text-[10px] font-medium text-green-600 flex items-center gap-1">
                        <span class="material-symbols-outlined text-[14px]">trending_up</span> +{{ newThisMonth }} this month
                    </div>
                </div>
            </div>
            <!-- More metrics here... -->
            <div class="bg-gradient-to-br from-secondary/10 to-teal-50/50 rounded-[2.5rem] p-1 w-full box-border">
                <div class="bg-white rounded-[2.4rem] p-8 h-full">
                    <div class="text-[10px] font-bold text-on-surface-variant tracking-[0.2em] uppercase mb-1">Security</div>
                    <div class="text-4xl font-extrabold text-secondary font-headline tracking-tighter">{{ pendingReports }}</div>
                    <div class="mt-4 text-[10px] font-medium text-red-500 flex items-center gap-1">
                        <span class="w-1.5 h-1.5 bg-red-500 rounded-full animate-pulse"></span> Active issues
                    </div>
                </div>
            </div>
            <!-- Uptime... -->
            <div class="bg-gray-50/50 rounded-[2.5rem] p-8">
                <div class="text-[10px] font-bold text-on-surface-variant tracking-[0.2em] uppercase mb-1">Health</div>
                <div class="text-4xl font-extrabold text-on-surface font-headline tracking-tighter">99.9%</div>
                <div class="mt-4 text-[10px] font-medium text-gray-400">System online</div>
            </div>
            <div class="bg-gray-50/50 rounded-[2.5rem] p-8">
                <div class="text-[10px] font-bold text-on-surface-variant tracking-[0.2em] uppercase mb-1">API Load</div>
                <div class="text-4xl font-extrabold text-on-surface font-headline tracking-tighter">Low</div>
                <div class="mt-4 text-[10px] font-medium text-gray-400">Stable latency</div>
            </div>
        </div>

        <div class="bg-white rounded-[3rem] p-12 shadow-sm relative overflow-hidden">
            <div class="absolute inset-0 bg-primary/5 [mask-image:radial-gradient(ellipse_at_top_right,black,transparent)]"></div>
            <div class="relative">
                <h3 class="text-2xl font-extrabold tracking-tight font-headline mb-8">Inscriptions mensuelles</h3>
                <div class="flex items-end gap-3 h-64">
                    {% for m in monthly %}
                        {% set height = (m.cnt / 20 * 100)|round %} {# Assuming max is around 20 for now #}
                        <div class="flex-1 group relative h-full flex flex-col justify-end items-center">
                            <div class="absolute -top-10 opacity-0 group-hover:opacity-100 transition-opacity bg-primary text-white text-[10px] font-bold px-2 py-1 rounded-full shadow-sm">
                                {{ m.cnt }}
                            </div>
                            <div class="w-full bg-primary/10 rounded-full group-hover:bg-primary/20 transition-all origin-bottom" style="height: {{ height }}%"></div>
                            <div class="mt-4 text-[9px] font-bold uppercase tracking-widest text-on-surface-variant transform -rotate-45 md:rotate-0">{{ m.month|split(' ')|first }}</div>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>

    <div x-show="activeTab === 'users'" x-transition:enter="transition ease-out duration-300" x-transition:enter-start="opacity-0 translate-y-4">
        <div class="bg-white rounded-[3rem] p-2 shadow-sm border border-outline/30">
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-1 p-2">
                {% for user in users %}
                <div class="flex flex-col p-8 rounded-[2.2rem] hover:bg-gray-50 group transition-all duration-300 border border-transparent hover:border-outline/50">
                    <div class="flex items-center gap-4 mb-6">
                        <div class="w-16 h-16 rounded-[1.25rem] bg-indigo-50 text-primary flex items-center justify-center font-bold text-xl group-hover:scale-105 transition-transform">
                            {{ user.first_name|first|upper }}{{ user.last_name|first|upper }}
                        </div>
                        <div>
                            <div class="text-lg font-extrabold text-on-surface leading-tight">{{ user.first_name }} {{ user.last_name }}</div>
                            <div class="text-[11px] text-on-surface-variant font-medium mt-0.5 truncate max-w-[140px]">{{ user.email }}</div>
                        </div>
                        <div class="ml-auto">
                            <button @click="blockUser({{ user.id }})" 
                                    class="p-3 rounded-2xl bg-white shadow-sm border border-outline/50 text-on-surface-variant hover:text-red-500 transition-all active:scale-90"
                                    title="{{ user.status == 'BLOCKED' ? 'Débloquer' : 'Bloquer' }}">
                                <span class="material-symbols-outlined text-[20px]">{{ user.status == 'BLOCKED' ? 'lock_open' : 'block' }}</span>
                            </button>
                        </div>
                    </div>
                    
                    <div class="flex items-center gap-3">
                        <span class="px-3.5 py-1.5 rounded-full text-[9px] font-bold uppercase tracking-[0.1em]
                            {% if 'ADMIN' in user.roles %} bg-purple-100/70 text-purple-700
                            {% elseif 'PSYCHOLOGUE' in user.roles %} bg-indigo-100/70 text-indigo-700
                            {% else %} bg-slate-100/70 text-slate-700
                            {% endif %}">
                            {{ user.roles|first|replace({'ROLE_': ''}) }}
                        </span>
                        <div class="flex-1 h-px bg-outline/20"></div>
                        <div class="flex items-center gap-1.5 text-[10px] font-bold {{ user.status == 'ACTIVE' ? 'text-green-500' : 'text-red-500' }}">
                            <span class="w-1.5 h-1.5 rounded-full {{ user.status == 'ACTIVE' ? 'bg-green-500' : 'bg-red-500' }}"></span>
                            {{ user.status }}
                        </div>
                    </div>
                </div>
                {% endfor %}
            </div>
        </div>
    </div>

    <div x-show="activeTab === 'messages'" x-transition:enter="transition ease-out duration-300" x-transition:enter-start="opacity-0 translate-y-4" class="space-y-6">
        <div class="flex items-center justify-between mb-2">
            <h3 class="text-2xl font-extrabold font-headline tracking-tighter">Signalements Privés</h3>
            <span class="text-xs font-bold text-gray-400 bg-gray-100 px-3 py-1 rounded-full">Dépôt sécurisé</span>
        </div>
        
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
            {% for report in reports|filter(r => r.context == 'MESSAGING') %}
            <div class="bg-white rounded-[2.5rem] p-8 shadow-sm border border-outline/20 group hover:shadow-md transition-all">
                <div class="flex items-start justify-between mb-6">
                    <div class="flex items-center gap-3">
                        <div class="w-10 h-10 rounded-full bg-red-50 text-red-500 flex items-center justify-center group-hover:rotate-12 transition-transform">
                            <span class="material-symbols-outlined">warning</span>
                        </div>
                        <div>
                            <div class="text-[10px] font-bold text-gray-400 uppercase tracking-widest">Report #{{ report.id }}</div>
                            <div class="text-xs font-bold text-red-600 tracking-wide">{{ report.reason }}</div>
                        </div>
                    </div>
                </div>
                
                <div class="grid grid-cols-2 gap-6 mb-8 p-4 bg-gray-50/50 rounded-2xl border border-outline/10">
                    <div>
                        <div class="text-[9px] font-bold text-gray-400 uppercase mb-1">Plaignant</div>
                        <div class="text-sm font-extrabold">{{ report.reporter_first }} {{ report.reporter_last }}</div>
                    </div>
                    <div>
                        <div class="text-[9px] font-bold text-gray-400 uppercase mb-1">Cible</div>
                        <div class="text-sm font-extrabold">{{ report.reported_first }} {{ report.reported_last }}</div>
                    </div>
                </div>
                
                {% if report.details %}
                <div class="mb-8 overflow-hidden rounded-2xl relative">
                    <div class="absolute top-0 left-0 w-1 h-full bg-primary/20"></div>
                    <div class="bg-gray-50/20 p-4 text-[11px] text-on-surface-variant leading-relaxed">
                        {{ report.details }}
                    </div>
                </div>
                {% endif %}
                
                <div class="flex items-center gap-3">
                    <button @click="resolveReport({{ report.id }}, 'IGNORE')" class="flex-1 py-3.5 text-xs font-bold text-on-surface-variant bg-gray-50 border border-outline/30 rounded-2xl hover:bg-white hover:border-outline transition-all active:scale-95 uppercase tracking-wider">Passer</button>
                    <button @click="resolveReport({{ report.id }}, 'BLOCK_USER')" class="flex-2 py-3.5 px-8 text-xs font-bold text-white bg-red-500 rounded-2xl hover:bg-black transition-all active:scale-95 shadow-lg shadow-red-100 uppercase tracking-wider">Bloquer le profil</button>
                </div>
            </div>
            {% else %}
            <div class="lg:col-span-2 py-20 bg-gray-50/30 rounded-[3rem] border-2 border-dashed border-outline/40 flex flex-col items-center justify-center text-center">
                <span class="material-symbols-outlined text-6xl text-gray-200 mb-4 scale-125">auto_sparkles</span>
                <h3 class="font-headline font-extrabold text-xl text-gray-400">Canal de messagerie fluide</h3>
                <p class="text-xs text-gray-400 mt-2 uppercase tracking-widest">Aucun signalement en attente de révision</p>
            </div>
            {% endfor %}
        </div>
    </div>

    <div x-show="activeTab === 'community'" x-transition:enter="transition ease-out duration-300" x-transition:enter-start="opacity-0 translate-y-4" class="space-y-8 opacity-60 grayscale hover:opacity-100 hover:grayscale-0 transition-all duration-700">
        <div class="bg-amber-50 rounded-[2.5rem] p-6 border border-amber-100 flex items-center gap-4 mb-4">
            <span class="material-symbols-outlined text-amber-600 animate-bounce">construction</span>
            <div class="text-sm font-bold text-amber-700 uppercase tracking-wider">Module en attente d'intégration API - Zone réservée</div>
        </div>
        
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
            <!-- Community Report Mockup 1 -->
            <div class="bg-white rounded-[3rem] p-10 border border-outline/20">
                <div class="flex gap-4 mb-8">
                    <div class="w-12 h-12 rounded-full bg-teal-50 text-teal-600 flex items-center justify-center font-bold">#48</div>
                    <div>
                        <h4 class="font-headline font-extrabold text-lg">Harcèlement dans Forum</h4>
                        <div class="text-[10px] text-gray-400 font-bold uppercase mt-1">Sujet: "Comment gérer le stress"</div>
                    </div>
                </div>
                
                <div class="space-y-6 mb-10">
                    <div class="p-5 bg-gray-50 rounded-[1.5rem] text-sm italic text-on-surface-variant">
                        "L'utilisateur AlexB a insulté plusieurs membres dans le thread principal."
                    </div>
                    
                    <div class="space-y-4">
                        <label class="text-[10px] font-extrabold uppercase tracking-widest text-on-surface-variant">Définir un Timeout (Sanction)</label>
                        <div class="flex gap-2">
                            <button class="px-5 py-2.5 rounded-full text-[10px] font-bold bg-amber-50 text-amber-700 hover:bg-amber-100 transition-colors uppercase">1 heure</button>
                            <button class="px-5 py-2.5 rounded-full text-[10px] font-bold bg-amber-50 text-amber-700 hover:bg-amber-100 transition-colors uppercase">24 heures</button>
                            <button class="px-5 py-2.5 rounded-full text-[10px] font-bold bg-red-50 text-red-700 hover:bg-red-100 transition-colors uppercase">Indéfini</button>
                        </div>
                    </div>
                </div>

                <div class="flex gap-4">
                    <button class="flex-1 py-4 text-xs font-extrabold uppercase bg-on-surface text-white rounded-[1.5rem] hover:bg-on-surface-variant transition-colors">Appliquer Sanction</button>
                    <button class="px-6 py-4 rounded-[1.5rem] border border-outline/50 hover:bg-gray-50 transition-colors">
                        <span class="material-symbols-outlined">delete</span>
                    </button>
                </div>
            </div>

            <!-- More Placeholders -->
            <div class="bg-white rounded-[3rem] p-10 border border-outline/20 flex flex-col items-center justify-center text-center group cursor-not-allowed">
                <div class="w-20 h-20 rounded-full bg-gray-50 flex items-center justify-center mb-6 group-hover:scale-110 transition-transform">
                    <span class="material-symbols-outlined text-4xl text-gray-300">add_moderator</span>
                </div>
                <h4 class="font-headline font-extrabold text-xl mb-2 text-gray-400">Nouveau Signalement</h4>
                <p class="text-[10px] text-gray-300 font-bold uppercase tracking-widest leading-loose">Waiting for community module<br>To be connected by backend team</p>
            </div>
        </div>
    </div>
</div>

<script>
function adminSystem() {
    return {
        activeTab: 'overview',
        async blockUser(userId) {
            if (!confirm('Changer le statut de cet utilisateur ?')) return;
            const res = await fetch(`/admin/users/block/${userId}`, { method: 'POST' });
            if (await res.json()) window.location.reload();
        },
        async resolveReport(reportId, action) {
            if (!confirm(`Action : ${action} ?`)) return;
            const res = await fetch(`/admin/reports/${reportId}/resolve`, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ action: action })
            });
            if (await res.json()) window.location.reload();
        }
    }
}
</script>
{% endblock %}
